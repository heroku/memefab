<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Memefab" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Fabricate Fabulous Memes with Memefab" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class='container'>
      <header class='clearfix'>
        <nav>
          <ul class="nav nav-pills float-right">
            <li class="nav-item">
              <%= link_to "Images", images_path, class: "nav-link #{'active' if current_page?(images_path)}" %>
            </li>
          </ul>
        </nav>
        <h3 class="text-muted">Memefab 🏭</h3>
      </header>

      <main role="main">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
